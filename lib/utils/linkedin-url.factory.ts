import { ServiceOptions } from '../oauth.interface';

export const createLinkedinLoginUrl = (options: ServiceOptions): string => {
  return '';
};

export const createLinkedinUserFunction = (options: ServiceOptions): any => {
  return;
};
