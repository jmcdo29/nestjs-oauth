import { ServiceOptions } from '../oauth.interface';

export const createFacebookLoginUrl = (options: ServiceOptions): string => {
  return '';
};

export const createFacebookUserFunction = (options: ServiceOptions): any => {
  return;
};