import { ServiceOptions } from '../oauth.interface';

export const createGithubLoginUrl = (options: ServiceOptions): string => {
  return '';
};

export const createGithubUserFunction = (options: ServiceOptions): any => {
  return;
}